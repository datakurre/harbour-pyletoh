[buildout]
parts = app

bin-directory = qml
eggs-directory = lib
parts-directory = lib
relative-paths = true

download-cache = requires

develop = .

[app]
recipe = zc.recipe.egg
eggs = harbour-pyletoh
# extra-paths = pycharm-debug-py3k.egg
